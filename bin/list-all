#!/usr/bin/env bash

set -euo pipefail

current_script_path=${BASH_SOURCE[0]}
plugin_dir=$(cd "$(dirname "$current_script_path")/.." && pwd)

# shellcheck source=../lib/utils.bash
source "${plugin_dir}/lib/utils.bash"

list_all_versions | awk '
	NR > 1 { printf " " }
	{ printf "%s", $0 }
	END { printf "\n" }
'

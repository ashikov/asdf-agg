#!/usr/bin/env bash

set -euo pipefail

case "${FAKE_LDD_MODE:-unknown}" in
gnu)
	printf 'ldd (GNU libc) 2.39\n'
	;;
musl)
	printf 'musl libc (x86_64)\nVersion 1.2.5\n'
	;;
unknown)
	printf 'unknown libc implementation\n'
	;;
*)
	exit 1
	;;
esac

#!/usr/bin/env bash

set -euo pipefail

case "${1:-}" in
-s)
	printf '%s\n' "$FAKE_UNAME_OS"
	;;
-m)
	printf '%s\n' "$FAKE_UNAME_ARCH"
	;;
*)
	printf 'unsupported fake uname argument: %s\n' "${1:-}" >&2
	exit 1
	;;
esac
